// Controller for the Profile page Header section
define([

    // Base controller module
    'ng_base/controller'

], function (

	ControllerModule

) {


    ControllerModule.controller('HeaderController', ['$scope', function ($scope) {
        // $scope.account = AccountService.getAccount();
    }]);
});
